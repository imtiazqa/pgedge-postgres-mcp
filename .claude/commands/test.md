---
allowed-tools: Bash(make:*)
description: Fully test the roject
---

# Test

Build all components using the Makefile.
Run all linters using the Makefile.
Run gofmt using the Makefile.
Run all tests using the Makefile.